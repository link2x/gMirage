// From modClientTCP

// Not much to say here. With how 39dll works it's about as
// unnecessary as it was in the original code.
// (To be honest, it's still a little /more/ unnecessary now.)

sendmessage(global.socket);
